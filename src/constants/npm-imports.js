export const React = `import React from 'react'`
export const ReactPure = `import React, { PureComponent } from 'react'`
export const ReactDOM = `import ReactDOM from 'react-dom'`
export const PropTypes = `import PropTypes from 'prop-types'`

export const Redux = `import { connect } from 'react-redux'`
export const mapStateToProps = `
const mapStateToProps = (state) => ({

})`
export const mapDispatchToProps = `
const mapDispatchToProps = (dispatch) => ({

})
`

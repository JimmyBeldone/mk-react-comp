const program = require('commander');

// Component arguments
export const isCss = program.css;
export const isStylus = program.styl;
export const isLess = program.less;
export const isScss = program.scss;

export const withJSX = program.jsx;
export const isStateless = program.stateless;

export const isEntry = program.entry;
export const withLifecycle = program.lifecycle;
export const withRedux = program.redux;

export const withFolder = program.dir;
export const withFull = program.full;

export const withPkg = program.pkg;

export const configFile = 'mkreactconfig.js';
